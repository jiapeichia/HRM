@model Meo.Web.ViewModels.SalesInvoiceViewModels

@{
    ViewBag.Title = "Invoice";
    var index = 1;
}

<head>
    <style>
        th, td {
            border-top: 1px solid #ddd;
            text-align: center;
        }

        legend {
            padding: 0.2em 0.5em;
            color: grey;
            font-size: 130%;
        }

        fieldset {
            border: 1px solid grey;
            padding: 1em;
            font: 100%/1 sans-serif;
        }

        label {
            float: left;
            width: 25%;
            margin-right: 0.5em;
            padding-top: 0.2em;
            text-align: right;
            font-weight: bold;
        }

        td {
            padding-left: 20px;
        }

        .k-upload .k-upload-button {
            min-width: 7.167em;
            margin-right: 0em;
        }

        .k-dropzone {
            border-style: solid;
            border-width: 0;
            padding: .2em;
            background-color: white;
        }

        .k-dropdown, .k-textbox {
            /*width: 100%;*/
            max-width: 100%;
            width: 18.4em;
        }

        .fontsize {
            font-size: 25px;
        }

        #loading-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: gray; /*rgba(255, 255, 255, 0.8);*/ /* semi-transparent white background */
        }

        #loading-spinner {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 5px solid #3498db; /* blue border */
            border-top: 5px solid gray; /*#ffffff;*/ /* white top border to create spinner effect */
            animation: spin 1s linear infinite; /* CSS animation for spinning */
        }
    </style>

    @section Scripts {
        <script>
            $(function () {
                $("#backinvoice").click(function () {
                    window.history.back();
                    @*window.open('@Url.Action("AllInvoice", "Report", new { salesId = Model.SalesId })', "_self");*@
                  });

                 $("#salesreport").click(function () {
                    window.open('@Url.Action("Search", "Report", new { reportType = "sales" @*salesId = Model.SalesId*@ })', "_self");
                 });

                 $("#servicereport").click(function () {
                    window.open('@Url.Action("Search", "Report", new { reportType = "service" @*salesId = Model.SalesId*@ })', "_self");
                 });

                //function goBack() {
                //    window.history.back();
                //}

                $("#gohistory").click(function () {
                    window.open('@Url.Action("CusPurchaseHistory", "Sales", new { id = Model.CusId })', "_self");
                });

                $("#cusservice").click(function () {
                    window.open('@Url.Action("SalesByCus", "Sales", new { id = Model.CusId })', "_self");
                });

                $("#close").click(function () {
                    //window.history.back();
                    window.open('@Url.Action("CusPurchaseHistory", "Sales", new { id = Model.CusId })', "_self");
                });

                $("#PrintReceipt").click(function () {
                    var salesId = '@Model.SalesId';
                    var url = '@Url.Action("Index", "Receipt")' + '?salesId=' + salesId;
                    const printWindow = window.open(url, '_blank');
                });

                $("#editInvoice").click(function () {
                    window.open('@Url.Action("EditInvoice", "Sales", new { salesid = Model.SalesId })', "_self");
                });

                $("#cancelInvoice").click(function () {
                    $("#cancelModal").modal("show");
                });

                $("#confirmCancel").click(function () {
                    var reason = $("#cancelReason").val().trim();
                    if (!reason) {
                        alert("Please enter a reason for cancellation.");
                        return;
                    }
                    $("#confirmCancel").prop("disabled", true);
                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("CancelInvoice", "Sales")',
                        data: { salesid: "@Model.SalesId", reason: reason },
                        success: function (response) {
                            $("#confirmCancel").prop("disabled", false);
                            if (response && response.success) {
                                alert("Invoice @Model.SalesId has been cancelled.");
                                window.open('@Url.Action("CusPurchaseHistory", "Sales", new { id = Model.CusId })', "_self");
                            } else {
                                alert("Error: " + (response.message || "Cancel failed."));
                            }
                        },
                        error: function () {
                            $("#confirmCancel").prop("disabled", false);
                            alert("A network error occurred. Please try again.");
                        }
                    });
                });

                @*$("#PrintReceipt").click(function () {

                    // Show loading icon
                    $('#loading-overlay').show();

                    $.ajax({
                        url: '@Url.Action("Index", "Receipt")',
                        type: "POST", // or "POST" depending on your server-side logic
                        data: {
                            salesId: "@Model.SalesId",
                        },
                        success: function (response) {
                            // Handle the response if needed
                            $('#loading-overlay').hide();
                        },
                        error: function (error) {
                            // Handle errors if the AJAX call fails
                        }
                    });
                });*@
            });

            $("html, body").animate({
                scrollTop: 0
            }, 200);

        </script>
    }
</head>
<body>
    <div id="loading-overlay">
        <div id="loading-spinner"></div>
    </div>
    <div class="page-content">
        <div class="col-md-12 mx-0">
            @{
                var _role = Session["RoleName"]?.ToString() ?? "";
                var _isAdminOrPic = _role.Contains("Admin") || _role.Equals("PIC");
            }
            <div align="right" style="width:100%">
                <input type="button" value="GO SERVICE" class="btn btn-default" id="cusservice" style="width:100px" />
                <input type="button" value="GO HISTORY" class="btn btn-default" id="gohistory" style="width:100px" />
                <input type="button" value="PRINT" class="btn btn-default" id="PrintReceipt" style="width:100px" />
                @if (_isAdminOrPic)
                {
                    <input type="button" value="EDIT" class="btn btn-success" id="editInvoice" style="width:80px" />
                    <input type="button" value="CANCEL INV" class="btn btn-danger" id="cancelInvoice" style="width:100px" />
                }
                @if (ViewData["From"].ToString() == "allinvoice")
                {
                    <input type="button" value="BACK" class="btn btn-default" id="backinvoice" style="width:100px" />
                }
                else if (ViewData["From"].ToString() == "servicereport")
                {
                    <input type="button" value="BACK" class="btn btn-default" id="servicereport" style="width:100px" />
                }
                else if (ViewData["From"].ToString() == "salesreport")
                {
                    <input type="button" value="BACK" class="btn btn-default" id="salesreport" style="width:100px" />
                }
                else
                {
                    <input type="button" value="BACK" class="btn btn-default" id="close" style="width:100px" />
                }
            </div>
            <br />

            @*// Sales Info*@
            <div class="sub-content" style="margin-top: 0px; padding-top: 20px">
                <div class="card px-0 pt-4 pb-0 mt-3 mb-3" style="text-align: center">
                    @if (Model.SalesId.StartsWith("CCT"))
                    {
                        <h2><strong>Credit Sales</strong></h2>
                    }
                    else if (Model.Exchange == true)
                    {
                        <h2><strong>Exchange</strong></h2>
                    }
                    else
                    {
                        <h2><strong>Sales Order</strong></h2>
                    }
                </div>
                <hr style="height:1px;border-width:0;color:gray;background-color:gray; margin:0px">
                <br />

                @Html.AntiForgeryToken()
                <!-- Header -->
                <table>
                    <tr>
                        <td style="border-top: none; text-align: left; padding: 3px; width: 10%;"><strong>Card No. </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px; width: 20%;">: @Model.CardNo </td>
                        <td style="border-top: none; text-align: left; padding: 3px; width: 10%;"><strong>Date </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px; width: 20%;">: @Model.OrderDate.ToShortDateString() </td>
                        <td style="border-top: none; text-align: left; padding: 3px"><strong>Invoice No. </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px">: @Model.SalesId</td>
                        <td style="border-top: none; text-align: left; padding: 3px"></td>
                    </tr>
                    <tr>
                        <td style="border-top:none; text-align:left; padding:3px">
                            <strong>Customer Name </strong>
                        </td>
                        <td style="border-top:none; text-align:left; padding:3px">
                            :
                            @ViewData["CustomerName"].ToString()
                            <input id="CusId" type="text" value="@Model.CusId" style="display:none; width:100%" />
                        </td>
                        <td style="border-top: none; text-align: left; padding: 3px"><strong>Paid By </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px">: @Model.PaymentMethodName </td>
                        <td style="border-top: none; text-align: left; padding: 3px"><strong>Total Discount: </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px">: @Model.DiscAmt</td>
                        <td style="border-top: none; text-align: left; padding: 3px"></td>
                    </tr>
                    <tr>
                        <td style="border-top: none; text-align: left; padding: 3px"><strong>Credit Balance </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px">: @Model.CreditBal </td>
                        <td style="border-top: none; text-align: left; padding: 3px"><strong>Remarks </strong></td>
                        <td style="border-top: none; text-align: left; padding: 3px" colspan="3">: @Model.Remarks</td>
                    </tr>
                </table>
                <br />

                <!-- Body [Sales Item] -->
                <table id="tblSalesItem" style="width:100%">
                    <tr>
                        <th>
                            No
                        </th>
                        <th>
                            Item
                        </th>
                        <th>
                            Remarks
                        </th>
                        <th>
                            Qty
                        </th>
                        <th>
                            Unit Price
                        </th>
                        <th>
                            Discount
                        </th>
                        <th>
                            Total
                        </th>
                        <th>
                            Sales By
                        </th>
                    </tr>
                    @if (Model.ExchangeDetails != null && Model.ExchangeDetails.Any())
                    {
                        foreach (var item in Model.ExchangeDetails)
                        {
                            <tr>
                                @*<td class="salesitemid" style="display:none"><span>@item.SalesItemId</span></td>*@
                                <td style="width:5%">@index</td>
                                <td style="width:20%; text-align:left">
                                    @Html.DisplayFor(modelItem => item.ProductName)
                                </td>
                                <td style="width:15%; text-align:left">
                                    @Html.DisplayFor(modelItem => item.Remarks)
                                </td>
                                <td class="quantity" style="width:5%">
                                    @if (Model.PaidAmt == null)
                                    {
                                        <span>@item.Quantity</span>
                                        <input type="text" value="@item.Quantity" style="display:none; width:100%" />
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    }
                                </td>
                                <td class="unitprice" style="width:10%">
                                    <span>@item.UnitPrice</span>
                                </td>
                                <td class="linediscount" style="width:10%">
                                    @if (Model.PaidAmt == null)
                                    {
                                        <span>@item.LineDiscAmt</span>
                                        <input type="text" value="@item.LineDiscAmt" style="display: none; width: 100%" />
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.LineDiscAmt)
                                    }
                                </td>
                                <td class="linetotal" style="width:10%">
                                    <span>@item.LineTotal</span>
                                </td>
                                <td style="width:10%">
                                    <span>@item.EmpName</span>
                                </td>
                            </tr>
                            index++;
                        }
                    }
                    @if (Model.SalesDetails != null && Model.SalesDetails.Any())
                    {
                        foreach (var item in Model.SalesDetails)
                        {
                            <tr>
                                <td class="salesitemid" style="display:none"><span>@item.SalesItemId</span></td>
                                <td style="width:5%">@index</td>
                                <td style="width:20%; text-align:left">
                                    @Html.DisplayFor(modelItem => item.ProductName)
                                </td>
                                <td style="width:15%; text-align:left">
                                    @Html.DisplayFor(modelItem => item.Remarks)
                                </td>
                                <td class="quantity" style="width:5%">
                                    @if (Model.PaidAmt == null)
                                    {
                                        <span>@item.Quantity</span>
                                        <input type="text" value="@item.Quantity" style="display:none; width:100%" />
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    }
                                </td>
                                <td class="unitprice" style="width:10%">
                                    <span>@item.UnitPrice</span>
                                </td>
                                <td class="linediscount" style="width:10%">
                                    @if (Model.PaidAmt == null)
                                    {
                                        <span>@item.LineDiscAmt</span>
                                        <input type="text" value="@item.LineDiscAmt" style="display: none; width: 100%" />
                                    }
                                    else
                                    {
                                        @Html.DisplayFor(modelItem => item.LineDiscAmt)
                                    }
                                </td>
                                <td class="linetotal" style="width:10%">
                                    <span>@item.LineTotal</span>
                                </td>
                                <td style="width:10%">
                                    <span>@item.EmpName</span>
                                </td>
                            </tr>
                            index++;
                        }
                    }
                    else
                    {
                        <tr><td colspan="8">No sales details available.</td></tr>
                    }
                    <tr>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td><strong>@Model.totalQty</strong></td>
                        <td></td>
                        <td><strong>@Model.totalDisc</strong></td>
                        <td><strong>@Model.subtotal</strong></td>
                        <td></td>
                    </tr>
                </table>
            </div>
        </div>
    </div>

    <!-- Cancel Invoice Confirmation Modal -->
    <div class="modal fade" id="cancelModal" tabindex="-1" role="dialog" aria-labelledby="cancelModalLabel">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header" style="background:#d9534f; color:#fff;">
                    <button type="button" class="close" data-dismiss="modal" style="color:#fff;">&times;</button>
                    <h4 class="modal-title" id="cancelModalLabel">Cancel Invoice @Model.SalesId</h4>
                </div>
                <div class="modal-body">
                    <p><strong>Warning:</strong> This will permanently cancel the invoice and reverse all associated stock, services, and customer credit changes. This action cannot be undone.</p>
                    <div class="form-group">
                        <label for="cancelReason"><strong>Reason for cancellation (required):</strong></label>
                        <textarea id="cancelReason" class="form-control" rows="3" placeholder="Enter reason..."></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-danger" id="confirmCancel">Confirm Cancel</button>
                </div>
            </div>
        </div>
    </div>
</body>